name: 'using_paper_button'
description: A tiny web app that uses a paper button.

dependencies:
  browser: ^0.10.0
  polymer_elements: ^1.0.0-rc.3
  polymer: ^1.0.0-rc.6
  web_components: ^0.12.0+3
  reflectable: ^0.3.3

transformers:
- web_components:
    entry_points: web/index.html

environment:
  sdk: '>=1.8.3 <2.0.0'
